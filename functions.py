import params
import classes

import numpy as np
from numba import njit, prange

@njit
def K_delta():
    return(0)

@njit(parallel=True)
def compute_bound_sheet():
    return(0)

@njit(parallel=True)
def advance_free_sheet():
    return(0)

@njit(parallel=True)
def compute_pressures():
    return(0)

@njit(parallel=True)
def compute_forces():
    return(0)

